
services:
  redis:
    image: redis:7
    container_name: rate-limiter-redis
    ports:
      - "6379:6379"
  
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    depends_on:
      - redis